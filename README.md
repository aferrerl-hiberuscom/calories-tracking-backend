# Calories Tracking Backend

Minimal scaffold for feature `018_api-rest-backend`.

## Run

1. Install dependencies: `npm install`
2. Copy env: `copy .env.example .env`
3. Start dev server: `npm run dev`
